/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  darkMode: ["class", '[data-theme="dark"]'],
  /*
   * Тем оформления здесь нет намеренно: весь вид задан в src/app/styles/App.css
   * переносом стилей оригинала. Завести цвета и здесь — получить два источника
   * правды, которые со временем разойдутся.
   *
   * Tailwind остаётся ради нормализации (preflight) и редких служебных утилит.
   */
  theme: {},
  /*
   * Свой .container у Tailwind выключен: в перенесённых стилях есть класс
   * с тем же именем, а утилиты идут в каскаде после компонентов и перебивали
   * его. Из-за этого на широком экране страница растягивалась до 1280px
   * вместо заложенных в макете 1120.
   */
  corePlugins: {
    container: false,
  },
  plugins: [],
};
