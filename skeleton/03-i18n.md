# 03. Переводы

## Правила

1. **Приложение не импортирует `i18next` напрямую** — только `@/shared/i18n`.
   Замена библиотеки должна остаться правкой этого модуля.
2. **Словари — TypeScript, по файлу на раздел сайта, все языки рядом**: забытый
   перевод заметен глазом.
3. **Пропущенный перевод — ошибка компиляции.** Ключи выводятся из языка-источника
   (`SOURCE_LOCALE`); остальные языки обязаны повторить набор. Недостающий ключ,
   лишний ключ, забытый язык и опечатка в `t()` не соберутся (проверено `tsc`).
4. **Всё, что создаётся при загрузке модуля, хранит ключ, а не текст**: схемы zod,
   списки меню, варианты выбора. Текст подставляется при отрисовке.
5. **Вне компонента переводит `translate`**, в компоненте — `useT`.
6. **Порядок выбора языка:** выбор в этом браузере → язык браузера → язык по умолчанию.
7. **Смена языка меняет `<html lang>`** — от него зависят переносы, форматы дат и скринридер.
8. **Сервер тексты интерфейса не переводит** — отдаёт коды ошибок, фразу собирает фронтенд.

## Структура

```
src/shared/i18n/
  index.ts              публичный API
  model/
    locale.ts           список языков, определение, хранение выбора
    instance.ts         экземпляр i18next (внутренний)
    useI18n.ts          useT, useLocale, useFieldError, translate
  locales/
    defineSection.ts
    index.ts            сборка разделов
    common.ts  errors.ts  validation.ts  …
```

## Код

```ts
// src/shared/i18n/model/locale.ts
// ── Настройка проекта ─────────────────────────────────────────
export const LOCALES = ["en", "ru"] as const;
export type Locale = (typeof LOCALES)[number];

/** Источник истины для набора ключей в словарях. */
export const SOURCE_LOCALE = "en" as const satisfies Locale;
export const DEFAULT_LOCALE: Locale = "en";

/** Название языка на нём самом. */
export const LOCALE_NAMES: Record<Locale, string> = {
  en: "English",
  ru: "Русский",
};

const STORAGE_KEY = "locale";
// ──────────────────────────────────────────────────────────────

export const isLocale = (value: unknown): value is Locale =>
  typeof value === "string" && (LOCALES as readonly string[]).includes(value);

export function readStoredLocale(): Locale | null {
  try {
    const saved = localStorage.getItem(STORAGE_KEY);
    return isLocale(saved) ? saved : null;
  } catch {
    return null;
  }
}

export function storeLocale(locale: Locale) {
  try {
    localStorage.setItem(STORAGE_KEY, locale);
  } catch {
    // язык проживёт до конца сеанса
  }
}

/** Выбор в браузере → язык браузера → по умолчанию. */
export function detectLocale(): Locale {
  const saved = readStoredLocale();
  if (saved) return saved;

  const tags = navigator.languages?.length ? navigator.languages : [navigator.language];
  for (const tag of tags) {
    const base = tag?.split("-")[0]?.toLowerCase();
    if (isLocale(base)) return base;
  }
  return DEFAULT_LOCALE;
}
```

```ts
// src/shared/i18n/model/instance.ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import { resources } from "../locales";
import { DEFAULT_LOCALE, detectLocale, storeLocale, type Locale } from "./locale";

void i18n.use(initReactI18next).init({
  resources,
  lng: detectLocale(),
  fallbackLng: DEFAULT_LOCALE,
  interpolation: { escapeValue: false }, // React экранирует сам
  returnNull: false,
});

export function changeLocale(locale: Locale) {
  void i18n.changeLanguage(locale);
  storeLocale(locale);
  document.documentElement.lang = locale;
}

document.documentElement.lang = i18n.language;

export { i18n };
```

```ts
// src/shared/i18n/model/useI18n.ts
import { useCallback } from "react";
import { useTranslation } from "react-i18next";
import { changeLocale, i18n } from "./instance";
import { DEFAULT_LOCALE, LOCALES, LOCALE_NAMES, isLocale, type Locale } from "./locale";
import type { Dictionary } from "../locales";

/** Ключ вида "common.save" — выводится из словаря. */
export type TranslationKey = {
  [S in keyof Dictionary]: `${S & string}.${keyof Dictionary[S] & string}`;
}[keyof Dictionary];

type Values = Record<string, string | number>;

/** Перевод вне компонента: язык на момент вызова. */
export const translate = (key: TranslationKey, values?: Values) =>
  i18n.t(key, values ?? {}) as string;

/** Перевод в компоненте. */
export function useT() {
  const { t } = useTranslation();
  return useCallback((key: TranslationKey, values?: Values) => t(key, values ?? {}) as string, [t]);
}

/** Ошибка поля формы: ключ "validation.*" → текст; прочие строки как есть. */
export function useFieldError() {
  const t = useT();
  return useCallback(
    (message?: string) => {
      if (!message) return undefined;
      return message.startsWith("validation.") ? t(message as TranslationKey) : message;
    },
    [t]
  );
}

/** Текущий язык и его смена. */
export function useLocale() {
  const { i18n: instance } = useTranslation();
  const locale: Locale = isLocale(instance.language) ? instance.language : DEFAULT_LOCALE;
  return {
    locale,
    locales: LOCALES,
    names: LOCALE_NAMES,
    setLocale: useCallback((next: Locale) => changeLocale(next), []),
  };
}
```

```ts
// src/shared/i18n/locales/defineSection.ts
import type { Locale, SOURCE_LOCALE } from "../model/locale";

type SourceLocale = typeof SOURCE_LOCALE;

export type Section<Keys extends string> = Record<Locale, Record<Keys, string>>;

/**
 * Раздел словаря. Ключи выводятся из языка-источника; остальные языки обязаны
 * повторить набор — иначе ошибка компиляции.
 */
export function defineSection<Keys extends string>(
  section: Record<SourceLocale, Record<Keys, string>> &
    Record<Exclude<Locale, SourceLocale>, Record<NoInfer<Keys>, string>>
): Section<Keys> {
  return section;
}
```

```ts
// src/shared/i18n/locales/common.ts
import { defineSection } from "./defineSection";

export const common = defineSection({
  en: {
    loading: "Loading…",
    save: "Save",
    cancel: "Cancel",
    themeLight: "Light theme",
    themeDark: "Dark theme",
  },
  ru: {
    loading: "Загрузка…",
    save: "Сохранить",
    cancel: "Отмена",
    themeLight: "Светлая тема",
    themeDark: "Тёмная тема",
  },
});
```

```ts
// src/shared/i18n/locales/errors.ts — ключи совпадают с ErrorCode бэкенда
import { defineSection } from "./defineSection";

export const errors = defineSection({
  en: {
    VALIDATION_FAILED: "Check the form fields",
    NOT_FOUND: "Not found",
    CONFLICT: "Conflict with existing data",
    INTERNAL: "Something went wrong",
    loadFailed: "Failed to load data",
  },
  ru: {
    VALIDATION_FAILED: "Проверьте правильность заполнения полей",
    NOT_FOUND: "Не найдено",
    CONFLICT: "Конфликт с существующими данными",
    INTERNAL: "Что-то пошло не так",
    loadFailed: "Не удалось загрузить данные",
  },
});
```

```ts
// src/shared/i18n/locales/validation.ts
import { defineSection } from "./defineSection";

export const validation = defineSection({
  en: { required: "Required field", tooLong: "Too long" },
  ru: { required: "Обязательное поле", tooLong: "Слишком длинно" },
});
```

```ts
// src/shared/i18n/locales/index.ts
import { LOCALES, type Locale } from "../model/locale";
import { common } from "./common";
import { errors } from "./errors";
import { validation } from "./validation";

/** Все разделы. Новый раздел достаточно дописать сюда. */
const SECTIONS = { common, errors, validation } as const;

type Sections = typeof SECTIONS;

/** Словарь одного языка. */
export type Dictionary = { [S in keyof Sections]: Sections[S][Locale] };

/** Хранится «раздел → язык», i18next ждёт «язык → раздел». */
export const resources = Object.fromEntries(
  LOCALES.map((locale) => [
    locale,
    {
      translation: Object.fromEntries(
        Object.entries(SECTIONS).map(([name, section]) => [name, section[locale]])
      ),
    },
  ])
) as Record<Locale, { translation: Dictionary }>;

/** Коды ошибок, у которых есть перевод. */
export const TRANSLATED_ERROR_CODES: ReadonlySet<string> = new Set(
  Object.keys(errors[LOCALES[0]])
);
```

```ts
// src/shared/i18n/index.ts
import "./model/instance";

export { useT, useLocale, useFieldError, translate } from "./model/useI18n";
export type { TranslationKey } from "./model/useI18n";

export { LOCALES, LOCALE_NAMES, DEFAULT_LOCALE, isLocale } from "./model/locale";
export type { Locale } from "./model/locale";

export type { Dictionary } from "./locales";
export { TRANSLATED_ERROR_CODES } from "./locales";
```

## Переключатель языка

```tsx
// src/features/Language/ui/LanguageSelect.tsx
import { useLocale, isLocale } from "@/shared/i18n";

export const LanguageSelect = () => {
  const { locale, locales, names, setLocale } = useLocale();

  return (
    <select value={locale} onChange={(e) => isLocale(e.target.value) && setLocale(e.target.value)}>
      {locales.map((code) => (
        <option key={code} value={code}>{names[code]}</option>
      ))}
    </select>
  );
};
```

## Новый язык

1. Добавить код в `LOCALES` и название в `LOCALE_NAMES`.
2. `tsc` покажет каждый раздел без нового языка — дописать переводы.
