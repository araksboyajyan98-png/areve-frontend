# Единые переводы строк в репозитории: иначе на Windows и Linux
# одни и те же файлы выглядят изменёнными целиком.
* text=auto eol=lf

# Двоичные файлы не трогать
*.webp binary
*.png binary
*.jpg binary
